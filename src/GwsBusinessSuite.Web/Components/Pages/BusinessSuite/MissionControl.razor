@page "/admin/mission-control"
@attribute [Authorize(Policy = "AdminOnly")]
@inject NavigationManager Navigation

@* Mission Control's KPI strip merged into the main dashboard at /admin (the 2026-09 design-plan
   nav regroup) - this route stays only so old bookmarks/links redirect instead of 404ing. *@

@code {
    protected override void OnInitialized() => Navigation.NavigateTo("/admin", forceLoad: false);
}
